name: Keep Supabase Alive
on:
    schedule:
        - cron: '*/10 * * * *'
jobs:
    ping:
        runs-on: ubuntu-latest
        steps:
            - name: Ping API
              run: curl -f https://quillert.vercel.app/api/keepalive
